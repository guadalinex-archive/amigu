# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/amigu.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall amigu
	if grep -qs '^amigu$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^amigu$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
